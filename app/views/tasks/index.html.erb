<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
  <%= link_to task.title, task_path(task) %>
  <%= link_to 'Delete', data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
  </li>
  <% end %>
</ul>



<%= link_to 'Add a new task', new_task_path %>

<%#= link_to 'Create a restaurant', restaurants_new_path %>
